/**
 * 계정 복구 경로 응급 킬 스위치.
 *
 * ## 왜 생겼나 (2026-08-27)
 *
 * `inspectSocialMbIdOccupant` 가 `generateSocialMbId` **해시만** 보고
 * "당신의 이전 계정입니다"라고 안내했다. 그 계정이 정말 이 소셜 신원에
 * 묶여 있는지(`g5_member_social_profiles`)를 확인하지 않았다.
 *
 * 해시는 `adler32(md5(identifier))` 인데, md5 를 **hex 문자열 32자**('0'~'f')로
 * 받아 adler32 를 걸기 때문에 출력 공간이 무너진다. 세 수치가 서로 다른 것을 재므로
 * 하나만 인용하면 안 된다:
 *
 * | 값 | 무엇 | 구하는 법 |
 * |---|---|---|
 * | 1/87 (4,930만) | 이론 조합 상한 | A 가짓수 1,729 × B 28,513 |
 * | 1/271 (1,586만) | 실제 도달 가능 출력 | DP 완전열거 |
 * | **약 205만** | **충돌 체감 키공간** | 40만개 생성 → 충돌 38,943건, 생일문제 역산 |
 *
 * 실사고: kakao 신원 5049479848 과 3427320197 이 둘 다 `kakao_8bf108d1` 로 해싱된다.
 * 그리고 `naver_989f08ee` 에서는 침입자가 원 주인의 닉네임까지 바꿨다(피해 확정 1건).
 *
 * ## 지금 상태
 *
 * ⭐ 구멍은 메워졌다 — `inspectSocialMbIdOccupant` 가 소유를 확인하고
 * `owned` 일 때만 복구를 내준다. 그래서 이 잠금은 **평상시 false** 다.
 *
 * ⛔ 지우지 않고 남겨 둔다. 복구 경로는 남의 계정을 통째로 넘길 수 있는 자리라
 * 다음에 문제가 보이면 코드 한 줄로 즉시 닫을 수 있어야 한다.
 * `true` 로 바꾸면 화면 진입·직접 POST·복구 액션이 모두 막히고 contact@ 로 안내된다.
 */
export const ACCOUNT_RECOVERY_LOCKED = false;

/** 잠금 중 회원에게 보여줄 문구. 원인을 노출하지 않는다. */
export const ACCOUNT_RECOVERY_LOCKED_MESSAGE =
    '이전 계정 복구 기능을 점검하고 있습니다. contact@damoang.net 으로 문의해 주시면 확인 후 도와드리겠습니다.';
